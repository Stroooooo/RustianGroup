# Run 
run these commands

Inside /dashboard-app run the following
```
npm i
npm run start
```

then open a second terminal tab and run this
inside /casanet-server/backend/
```
npm i
npm run serve
```